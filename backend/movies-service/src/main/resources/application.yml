server:
  port: 8081

spring:
  application:
    name: movies-service
  datasource:
    url: jdbc:h2:mem:moviesdb;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  cloud:
    consul:
      enabled: false
  sql:
    init:
      mode: never

security:
  api:
    enabled: false
    header: X-API-KEY
    key: dev-local-key

springdoc:
  swagger-ui:
    path: /swagger-ui

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
